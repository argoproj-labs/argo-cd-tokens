apiVersion: argoprojlabs.argoproj-labs.io/v1
kind: Token
metadata:
  name: token-sample
  namespace: argocd
spec:
  # Add fields here
  project: foobar
  role: testBar
  secretRef:
    name: asdf
    key: asdf